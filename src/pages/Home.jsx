import React from "react";
import ComicsList from '../components/ComicsList';

const Home = () => {
    return (
        <div className="home">
            <ComicsList />
        </div>
    );
};

export default Home;